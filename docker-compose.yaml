services:
  kysely-postgis:
    build:
      context: e2e/docker
      dockerfile: postgresql-15-postgis-3.dockerfile
    ports:
      - '15003:5432'
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: db
    restart: unless-stopped
